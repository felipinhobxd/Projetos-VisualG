algoritmo "Nome do Programa"

var

   n, i, final : inteiro

inicio

    final <- 0
    escreva("Deseja a tabuada para qual valor: ")
    leia(n)
        para i de 1 ate 10 faca
            final <- n * i
            escreval(n, "*", i, " = ", final)
         fimpara

fimalgoritmo